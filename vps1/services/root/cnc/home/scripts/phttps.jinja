#!/bin/sh
PORT=$1
shift
sudo "$HOME/.local/share/pnpm/bin/http-server" "$@" \
	-a {{ vps1.tailnet_ipv4 }} \
	-p $PORT \
	--log-ip \
	--ssl \
	--cert ~/ssl/fullchain.pem \
	--key ~/ssl/privkey.pem \
	--robots
